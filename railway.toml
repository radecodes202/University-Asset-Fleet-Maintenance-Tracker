[build]
builder = "nixpacks"

[deploy]
startCommand = "pip install -r requirements.txt && python manage.py collectstatic --no-input && python manage.py migrate"
healthcheckPath = "/login/"
restartPolicyType = "on_failure"